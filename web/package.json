{
  "name": "visuali2e-web",
  "private": true,
  "type": "module",
  "description": "Web-first Visuali2e (Fas 0: portad domän-IP + grindar). Se ../l-s-in-dig-p-jolly-harp-planen.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b --pretty",
    "build": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
